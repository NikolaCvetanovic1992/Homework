import { createStyles } from "@mui/styles";

export default createStyles({
  container: {
    position: "fixed",
    left: 0,
    bottom: 0,
    width: "100%",
    height: 50,
    backgroundColor: "white",
  },
});
